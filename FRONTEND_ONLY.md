# 🎨 Работа только с фронтендом (без бэкенда)

Этот гайд поможет вам разрабатывать UI/UX и страницы без необходимости запускать весь бэкенд.

## 🚀 Быстрый старт

### 1. Установите зависимости (если еще не установлены)

```bash
cd frontend
npm install
```

### 2. Создайте `.env.local` файл (опционально)

Файл `.env.local` не обязателен для работы без бэкенда. Приложение автоматически обработает отсутствие бэкенда.

Если хотите настроить переменные окружения, создайте `frontend/.env.local`:

```env
# API Configuration (не используется без бэкенда, но можно оставить)
NEXT_PUBLIC_API_URL=http://localhost:8080

# Keycloak Configuration (не используется без бэкенда)
NEXT_PUBLIC_KEYCLOAK_URL=http://localhost:8080/auth
NEXT_PUBLIC_KEYCLOAK_REALM=metacode
NEXT_PUBLIC_KEYCLOAK_CLIENT_ID=frontend-app
```

### 3. Запустите dev сервер

```bash
npm run dev
```

Откройте **http://localhost:3000** в браузере.

---

## ✅ Что работает без бэкенда

- ✅ Все страницы и роутинг
- ✅ UI компоненты и стили
- ✅ Навигация между страницами
- ✅ Формы и валидация на клиенте
- ✅ Layout и структура страниц
- ✅ Tailwind CSS стилизация

## ⚠️ Что НЕ работает без бэкенда

- ❌ Регистрация/вход (API запросы)
- ❌ Социальная авторизация
- ❌ Верификация email
- ❌ Восстановление пароля
- ❌ Любые запросы к API

---

## 🎯 Стратегии разработки

### Вариант 1: Игнорировать ошибки API (Текущий подход)

Фронтенд уже настроен так, что при отсутствии бэкенда:
- Приложение не падает
- Показываются ошибки в консоли (но это нормально)
- Можно разрабатывать UI/UX без проблем

**Просто запустите:**
```bash
npm run dev
```

И работайте с интерфейсом. Ошибки API в консоли можно игнорировать.

### Вариант 2: Использовать моки (Рекомендуется для разработки)

Можно создать моки API для более реалистичной разработки. См. раздел "Моки API" ниже.

---

## 📁 Структура для разработки страниц

Следуйте архитектуре FSD (Feature-Sliced Design):

```
src/
├── app/              # Страницы (роутинг)
│   └── (auth)/      # Страницы авторизации
│   └── page.tsx     # Главная страница
│
├── pages/           # Композиция страниц (если нужно)
│
├── widgets/         # Крупные блоки UI
│   └── Header/
│   └── Sidebar/
│
├── features/        # Бизнес-действия
│   └── auth/
│
└── shared/          # Переиспользуемые компоненты
    └── ui/          # UI компоненты (Button, Input и т.д.)
```

### Пример: Создание новой страницы

1. **Создайте страницу в `app/`:**
```typescript
// app/dashboard/page.tsx
export default function DashboardPage() {
  return (
    <div className="container mx-auto p-4">
      <h1 className="text-2xl font-bold">Dashboard</h1>
      {/* Ваш контент */}
    </div>
  );
}
```

2. **Создайте виджеты в `widgets/`:**
```typescript
// widgets/dashboard-stats/index.tsx
export function DashboardStats() {
  return (
    <div className="grid grid-cols-3 gap-4">
      {/* Статистика */}
    </div>
  );
}
```

3. **Используйте shared компоненты:**
```typescript
import { Button } from "@/shared/ui/Button";
import { Input } from "@/shared/ui/Input";
```

---

## 🎨 Работа с дизайном

### Tailwind CSS

Проект использует Tailwind CSS 4. Все стили пишутся через классы:

```tsx
<div className="flex items-center justify-between p-4 bg-white dark:bg-black rounded-lg shadow-md">
  <h2 className="text-xl font-semibold text-gray-900 dark:text-white">
    Заголовок
  </h2>
  <Button variant="primary">Действие</Button>
</div>
```

### Темная тема

Поддерживается автоматически через `dark:` префикс:

```tsx
<div className="bg-white dark:bg-gray-900 text-black dark:text-white">
  Контент с поддержкой темной темы
</div>
```

### Существующие UI компоненты

- `Button` - кнопки (`@/shared/ui/Button`)
- `Input` - поля ввода (`@/shared/ui/Input`)

Можно использовать их как основу для новых компонентов.

---

## 🔧 Полезные команды

```bash
# Запуск dev сервера
npm run dev

# Линтинг кода
npm run lint

# Форматирование кода
npm run format

# Сборка для production (проверка)
npm run build
```

---

## 📝 Примеры страниц для разработки

### 1. Профиль пользователя
```typescript
// app/profile/page.tsx
export default function ProfilePage() {
  return (
    <div className="max-w-4xl mx-auto p-6">
      <h1 className="text-3xl font-bold mb-6">Профиль</h1>
      {/* Форма профиля */}
    </div>
  );
}
```

### 2. Дашборд с интеграциями
```typescript
// app/dashboard/page.tsx
export default function DashboardPage() {
  return (
    <div className="container mx-auto p-6">
      <h1 className="text-2xl font-bold mb-6">Дашборд</h1>
      {/* GitHub интеграция */}
      {/* LeetCode интеграция */}
      {/* MonkeyType интеграция */}
    </div>
  );
}
```

### 3. Страница настроек
```typescript
// app/settings/page.tsx
export default function SettingsPage() {
  return (
    <div className="max-w-2xl mx-auto p-6">
      <h1 className="text-2xl font-bold mb-6">Настройки</h1>
      {/* Настройки аккаунта */}
    </div>
  );
}
```

---

## 🐛 Отладка

### Проблемы с загрузкой

Если приложение долго загружается при старте:
- Это нормально - AuthProvider пытается подключиться к бэкенду
- Через несколько секунд приложение загрузится
- Ошибки в консоли можно игнорировать

### Ошибки в консоли

Ожидаемые ошибки при работе без бэкенда:
- `Failed to fetch` - бэкенд недоступен (нормально)
- `NetworkError` - сетевые ошибки (нормально)
- `401 Unauthorized` - нет авторизации (нормально)

Все эти ошибки обрабатываются корректно и не ломают приложение.

---

## 💡 Советы

1. **Используйте TypeScript** - все типы уже настроены
2. **Следуйте FSD архитектуре** - см. `FSD_RULES.md`
3. **Используйте существующие компоненты** - не изобретайте велосипед
4. **Тестируйте в браузере** - Hot Reload работает автоматически
5. **Используйте DevTools** - React DevTools для отладки компонентов

---

## 🔄 Когда понадобится бэкенд

Бэкенд нужен для тестирования:
- Регистрации и входа
- Интеграций с GitHub/LeetCode
- Реальных API запросов
- Авторизации и токенов

Для этого используйте инструкцию из `QUICK_START.md`.

---

## 📚 Дополнительные ресурсы

- [FSD Rules](./FSD_RULES.md) - правила архитектуры
- [Next.js Docs](https://nextjs.org/docs) - документация Next.js
- [Tailwind CSS](https://tailwindcss.com/docs) - документация Tailwind
- [TypeScript](https://www.typescriptlang.org/docs/) - документация TypeScript

