/**
 * Layout для страниц авторизации
 * Route group (auth) не влияет на URL структуру
 */

export default function AuthLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
